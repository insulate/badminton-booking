# แผน: ปรับปรุงหน้า Booking เป็น Table UI

## เป้าหมาย

เปลี่ยน UI หน้า `/booking` (CustomerBookingPage) จาก list เป็น table สวยๆ พร้อม Progress Bar แสดงจำนวนสนามว่าง

---

## สรุปความต้องการ

- **รูปแบบ**: Dark/Modern Theme (เข้ากับ UI ปัจจุบัน)
- **Progress Bar**: แสดง % สนามว่าง พร้อม color coding
- **การจอง**: กดปุ่ม → เปิด Modal เลือก duration + ยืนยัน
- **Authentication**: ถ้ายังไม่ login → redirect ไป login ก่อน
- **Mobile**: ซ่อน Progress Bar, แสดงเฉพาะข้อมูลจำเป็น

---

## ไฟล์ที่ต้องแก้ไข

| # | ไฟล์ | ประเภท | รายละเอียด |
|---|------|--------|------------|
| 1 | `frontend/src/pages/customer/CustomerBookingPage.jsx` | แก้ไข | เปลี่ยน UI เป็น Table |
| 2 | `frontend/src/components/customer/BookingSlotModal.jsx` | สร้างใหม่ | Modal เลือก duration + จอง |

---

## รายละเอียดการแก้ไข

### 1. CustomerBookingPage.jsx

#### UI Table

**คอลัมน์:**

| คอลัมน์ | Desktop | Mobile | รายละเอียด |
|---------|---------|--------|------------|
| เวลา | ✅ | ✅ | แสดง startTime + icon 🔥 ถ้า Peak Hour |
| สถานะ | ✅ | ❌ ซ่อน | Progress Bar แสดง % สนามว่าง |
| ว่าง | ✅ | ✅ | แสดง "6/8 ว่าง" หรือ "เต็ม" |
| ราคา | ✅ | ✅ | แสดงราคา/ชม. |
| ปุ่มจอง | ✅ | ✅ | ปุ่ม "จอง" หรือ disabled ถ้าเต็ม |

#### Progress Bar Color Coding

| สถานะ | เงื่อนไข | สี |
|-------|---------|-----|
| ว่างมาก | > 50% | 🟢 เขียว (`bg-green-500`) |
| ว่างปานกลาง | 20-50% | 🟡 เหลือง (`bg-yellow-500`) |
| ว่างน้อย/เต็ม | < 20% | 🔴 แดง (`bg-red-500`) |

#### Flow การกดปุ่ม "จอง"

```
กดปุ่ม [จอง]
    │
    ├─ ถ้า login แล้ว (isAuthenticated = true)
    │   └─ เปิด BookingSlotModal พร้อมข้อมูล slot
    │
    └─ ถ้ายังไม่ login (isAuthenticated = false)
        └─ navigate(`/login?redirect=/booking`)
```

---

### 2. BookingSlotModal.jsx (สร้างใหม่)

#### Props

```jsx
{
  isOpen: boolean,
  onClose: () => void,
  slot: {
    timeSlotId: string,
    startTime: string,
    endTime: string,
    availableCount: number,
    totalCourts: number,
    peakHour: boolean,
    pricing: { normal: number, member: number }
  },
  selectedDate: Date,
  player: { name, phone, isMember },
  availability: array, // สำหรับคำนวณ max duration
  onSuccess: (booking) => void
}
```

#### ฟีเจอร์

1. **แสดงข้อมูล Slot**
   - วันที่ (format: วันอาทิตย์ที่ 8 ธันวาคม 2567)
   - เวลาเริ่มต้น
   - ราคา/ชม. (แสดงราคา member ถ้า player.isMember)
   - Peak Hour badge (ถ้ามี)

2. **เลือก Duration**
   - Dropdown หรือ Button group: 1, 2, 3, ... ชม.
   - Max duration = จำนวน slot ว่างติดกันต่อจาก slot ที่เลือก (สูงสุด 8 ชม.)
   - แสดงเวลาสิ้นสุด (คำนวณจาก startTime + duration)

3. **สรุปราคา**
   - แสดงราคารวม = ราคา/ชม. × duration
   - แสดง label "ราคาสมาชิก" ถ้า player.isMember

4. **ข้อมูลผู้จอง**
   - ชื่อ: player.name
   - เบอร์โทร: player.phone

5. **ปุ่ม Actions**
   - "ยกเลิก" → ปิด Modal
   - "ยืนยันการจอง" → เรียก API `customerBookingsAPI.create()` → แสดง Success Modal

---

## Design Mockup

### Desktop View

```
┌─────────────────────────────────────────────────────────────────┐
│  🏸 จองสนาม                                                     │
│  ───────────────────────────────────────────────────────────    │
│           ◀   08/12/2567   ▶                                    │
│        วันอาทิตย์ที่ 8 ธันวาคม 2567                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┬──────────────────────┬──────────┬────────┬──────┐│
│  │  เวลา    │       สถานะ          │   ว่าง   │  ราคา  │      ││
│  ├──────────┼──────────────────────┼──────────┼────────┼──────┤│
│  │ 08:00    │ ████████████░░░ 75%  │  6/8     │  ฿200  │ จอง  ││
│  │ 09:00    │ ██████████░░░░░ 50%  │  4/8     │  ฿200  │ จอง  ││
│  │ 10:00    │ █████░░░░░░░░░░ 25%  │  2/8     │  ฿200  │ จอง  ││
│  │ 17:00 🔥 │ ███░░░░░░░░░░░░ 25%  │  2/8     │  ฿300  │ จอง  ││
│  │ 18:00 🔥 │ █░░░░░░░░░░░░░░ 12%  │  1/8     │  ฿300  │ จอง  ││
│  │ 20:00 🔥 │ ░░░░░░░░░░░░░░░  0%  │  เต็ม   │  ฿300  │  -   ││
│  └──────────┴──────────────────────┴──────────┴────────┴──────┘│
│                                                                 │
│  🟢 ว่างมาก (>50%)   🟡 ว่างปานกลาง   🔴 ว่างน้อย   🔥 Peak Hour │
└─────────────────────────────────────────────────────────────────┘
```

### Mobile View

```
┌─────────────────────────────┐
│  🏸 จองสนาม                 │
│  ◀  08/12/67  ▶             │
│  อา. 8 ธ.ค. 67              │
├─────────────────────────────┤
│ ┌─────────┬───────┬───────┐ │
│ │  เวลา   │ ว่าง  │ ราคา  │ │
│ ├─────────┼───────┼───────┤ │
│ │ 08:00   │ 6/8   │ ฿200  │ │
│ │ 09:00   │ 4/8   │ ฿200  │ │
│ │ 17:00🔥 │ 2/8   │ ฿300  │ │
│ │ 20:00🔥 │ เต็ม  │ ฿300  │ │
│ └─────────┴───────┴───────┘ │
│                             │
│ 🟢 ว่างมาก 🟡 น้อย 🔴 เต็ม  │
└─────────────────────────────┘
```

### BookingSlotModal

```
┌─────────────────────────────────────┐
│              จองสนาม            [X] │
├─────────────────────────────────────┤
│                                     │
│  📅 วันอาทิตย์ที่ 8 ธันวาคม 2567     │
│  🕐 เริ่ม 17:00 น. 🔥 Peak Hour     │
│  💰 ราคา 300 บาท/ชม.               │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  จำนวนชั่วโมง                        │
│  ┌─────┬─────┬─────┬─────┐         │
│  │ 1   │ 2   │ 3   │ 4   │         │
│  └─────┴─────┴─────┴─────┘         │
│                                     │
│  🕐 17:00 - 19:00 (2 ชั่วโมง)       │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  👤 ผู้จอง: สมชาย ใจดี               │
│  📱 เบอร์โทร: 081-234-5678          │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  ┌─────────────────────────────┐   │
│  │       ยอดรวม: ฿600         │   │
│  │     * ราคาสมาชิก           │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌───────────┐  ┌───────────────┐  │
│  │   ยกเลิก  │  │ ยืนยันการจอง  │  │
│  └───────────┘  └───────────────┘  │
│                                     │
└─────────────────────────────────────┘
```

---

## ลำดับการทำงาน

| Step | Task | รายละเอียด |
|------|------|------------|
| 1 | สร้าง `BookingSlotModal.jsx` | Modal component ใหม่ |
| 2 | แก้ไข `CustomerBookingPage.jsx` | เปลี่ยนเป็น Table UI + เรียก Modal |
| 3 | ทดสอบ Desktop | ตรวจสอบ Table + Progress Bar + Modal |
| 4 | ทดสอบ Mobile | ตรวจสอบ responsive (ซ่อน Progress Bar) |
| 5 | ทดสอบ Flow จอง | ทดสอบ login redirect + จองสำเร็จ |

---

## Technical Notes

### คำนวณ Max Duration

```javascript
const calculateMaxDuration = (selectedSlotIndex, availability) => {
  let maxDuration = 1;
  for (let i = selectedSlotIndex; i < availability.length && i < selectedSlotIndex + 8; i++) {
    if (availability[i].availableCount < 1) break;
    maxDuration = i - selectedSlotIndex + 1;
  }
  return maxDuration;
};
```

### คำนวณ End Time

```javascript
const calculateEndTime = (startTime, duration, availability, slotIndex) => {
  const endIndex = slotIndex + duration - 1;
  return availability[endIndex]?.endTime || startTime;
};
```

### Progress Bar Percentage

```javascript
const percentage = (availableCount / totalCourts) * 100;
const colorClass = percentage > 50 ? 'bg-green-500' 
                 : percentage >= 20 ? 'bg-yellow-500' 
                 : 'bg-red-500';
```

---

## Dependencies

- ใช้ components ที่มีอยู่แล้ว:
  - `BookingSuccessModal.jsx` - แสดงหลังจองสำเร็จ
  - `customerBookingsAPI` - เรียก API จอง
  - `usePlayerAuthStore` - ตรวจสอบ login status

---

## Execution

หลังจากยืนยันแผนแล้ว ให้ implement ตามลำดับ:

1. สร้าง `BookingSlotModal.jsx`
2. แก้ไข `CustomerBookingPage.jsx`
3. ทดสอบ
